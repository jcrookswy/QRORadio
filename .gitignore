# Build output
Debug/
Release/
x64/

# Visual Studio
.vs/
*.vcxproj.user

# Runtime config / debug data
comport.txt
debug.txt
debugData.csv
compress.txt

# Archives
*.zip

# Personal/local settings (auto-generated by CRadio::SaveSettings(), contains
# COM port, callsign, and VNA calibration data specific to this machine/radio)
settings.json
settings2.json
settings - badunit.json

# Contest logs / QSO data (callsigns, personal station info)
log.adi
log.txt

# TX power-plot data dumps (generated debug output, like debugData.csv above)
avgpower.csv
avgpowerplot1.csv

# User manual (personal working doc) and its LibreOffice lock file
UserManual.odt
.~lock.*#

# Old CRadio.cpp backup snapshots (unused, not part of the build - see CLAUDE.md)
CRadioBroken.cpp
CRadiotemp.cpp

# txtest/ build artifacts and generated audio (keep the .cpp/.h/.bat source)
txtest/*.obj
txtest/*.exe
txtest/*.wav
txtest/err.txt
txtest/out.txt
txtest/stdout.txt
txtest/stderr.txt
